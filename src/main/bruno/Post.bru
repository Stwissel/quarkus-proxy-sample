meta {
  name: Post
  type: http
  seq: 2
}

post {
  url: {{server}}/proxy
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "url" : "https://wissel.net/"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
